export type TUser = {
        id: number;
        documentId: string;
        username: string;
        email: string;
        provider: string;
        confirmPassword: boolean;
        createdAt: string;
        updatedAt: string | null;

};
